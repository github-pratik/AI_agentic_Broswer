{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "installCommand": "npm ci --omit=optional",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}